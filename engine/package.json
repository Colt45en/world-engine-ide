{
  "name": "world-engine-geometry-backend",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "native:build": "node-gyp rebuild --directory native",
    "native:clean": "node-gyp clean --directory native",
    "native:smoke": "node scripts/smoke_native.mjs"
  },
  "devDependencies": {
    "node-gyp": "^10.2.0",
    "typescript": "^5.3.0"
  }
}
